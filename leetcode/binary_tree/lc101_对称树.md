# 🌲 LeetCode 101 - Symmetric Tree （对称二叉树）

## 📘 题目描述

给你一个二叉树的根节点 `root`，判断该二叉树是否是**轴对称的（镜像对称）**。

## 💡 解题思路

### 🧠 核心思想：**递归比较左右子树**

对称二叉树的特征：
- 左子树的左节点 = 右子树的右节点  
- 左子树的右节点 = 右子树的左节点  

因此我们可以通过递归同时遍历左右子树进行“镜像比较”。

### ⚙️ 递归逻辑

定义一个辅助函数 `compare(left, right)` 来判断：
1. 如果两者都为空 → ✅ 对称  
2. 如果只有一边为空 → ❌ 不对称  
3. 如果两者值不同 → ❌ 不对称  
4. 否则递归判断：
   - 外侧是否对称：`compare(left.left, right.right)`
   - 内侧是否对称：`compare(left.right, right.left)`
   - 两者都为真时才返回 True。

---

## 🧱 代码实现

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def isSymmetric(self, root: Optional[TreeNode]) -> bool:
        if not root:
            return True
        return self.compare(root.left, root.right)
        
    def compare(self, left: Optional[TreeNode], right: Optional[TreeNode]) -> bool:
        # 两个节点都为空 -> 对称
        if not left and not right:
            return True
        # 一个为空另一个不为空 -> 不对称
        elif not left or not right:
            return False
        # 节点值不同 -> 不对称
        elif left.val != right.val:
            return False
        # 递归判断外侧与内侧
        else:
            
            bool_out = self.compare(left.left,right.right)
            bool_in = self.compare(left.right,right.left)

            return True if bool_out and bool_in else False

