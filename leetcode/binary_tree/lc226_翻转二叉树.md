# 🌲 LeetCode 226 - Invert Binary Tree

## 📘 题目描述

给定一棵二叉树，翻转这棵树的左右子树，使得每个节点的左子树和右子树互换。


## 💡 解题思路

### 🧠 核心思想
这是一道经典的 **二叉树递归遍历** 题。  
翻转二叉树的本质操作是：**对每个节点交换其左右子树**。  
而要实现对整棵树的翻转，只需：
1. 递归地翻转左子树；
2. 递归地翻转右子树；
3. 在当前节点交换左右指针。

因为递归的底层实际上是通过 **栈结构（深度优先搜索 DFS）** 实现的，  
所以这是一个标准的 **前序遍历 (root → left → right)** 递归过程。

---

## 🧱 算法步骤详解

1. **终止条件**：如果当前节点为空，直接返回；
2. **交换左右子树**；
3. **递归处理左子树与右子树**；
4. **返回根节点**。

---

## 🧩 代码实现

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        if not root:
            return root
        
        # 交换左右子树
        root.left, root.right = root.right, root.left
        
        # 递归反转左右子树
        self.invertTree(root.left)
        self.invertTree(root.right)
        
        return root