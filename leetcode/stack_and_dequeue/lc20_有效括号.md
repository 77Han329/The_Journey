# Valid Parentheses

## 📖 题目链接
[LeetCode 20. Valid Parentheses](https://leetcode.com/problems/valid-parentheses/)



## 📌 思路
- 使用 **栈** 来追踪括号的匹配关系。
- 遍历字符串：
  - 如果遇到左括号 `(`、`{`、`[`，就把对应的“期望右括号”压入栈。
  - 如果遇到右括号 `)`、`}`、`]`，则检查栈顶是否有元素，且与当前字符匹配：
    - 如果不匹配 → 返回 `False`
    - 如果匹配 → 弹出栈顶
- 遍历完成后，如果栈为空，说明所有括号都合法匹配；否则返回 `False`。



## 🧩 代码实现
```python
class Solution:
    def isValid(self, s: str) -> bool:
        stack = []

        for item in s:
            if item == '(':
                stack.append(')')
            elif item == '{':
                stack.append('}')
            elif item == '[':
                stack.append(']')
            else:
                # 遇到右括号时检查是否匹配
                # 情况1: stack 为空，证明不匹配
                # 情况2: 当我们的item 是） 但是栈顶不是） 那就是错了 
                if not stack or stack.pop() != item:
                    return False
        
        # 栈为空才是完全匹配
        
        return True if not stack else False
        