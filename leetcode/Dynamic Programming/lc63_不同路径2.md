# 不同路径2

## 📖 题目链接
[LeetCode 62. Unique Paths2](https://leetcode.com/problems/unique-paths-ii/)




## 🧩 代码实现
```python
class Solution:
    def uniquePathsWithObstacles(self, obstacleGrid: List[List[int]]) -> int:
        # 确定dp 数组
        m,n = len(obstacleGrid), len(obstacleGrid[0])
        dp = [[0]*n for _ in range(m)]
        # 边界处理
        if obstacleGrid[0][0] == 1 or obstacleGrid[m-1][n-1] == 1:
            return 0
        #初始化 dp数组
        for i in range(m):
            if obstacleGrid[i][0] == 0:
                dp[i][0] = 1
            else:
                break
        for j in range(n):
            if obstacleGrid[0][j] == 0:
                dp[0][j] = 1
            else:
                break
        # 遍历顺序
        for i in range(1,m):
            for j in range(1,n):
                # 递推公式
                if obstacleGrid[i][j] == 1:
                    continue
                dp[i][j] = dp[i - 1][j] + dp[i][j - 1]
        return dp[m-1][n-1]
        
        