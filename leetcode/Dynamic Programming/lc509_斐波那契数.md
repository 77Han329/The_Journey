# 斐波那契数

## 📖 题目链接




## 📌 思路
1. 确定dp数组
    - dp[i]
2. 确定递推公式
    - dp[i] = dp[i-1] + dp[i-2]
3. dp数组的初始化
    - dp[0] = 1
    - dp[1] = 1
4. 遍历顺序
    - 从前向后遍历
5. 打印dp数组



## 🧩 代码实现
```python
class Solution:
    def fib(self, n: int) -> int:
        if n < 2:
            return n

        # 确定dp 数组
        dp = [0] * (n + 1)

        # dp数组的初始化
        dp[0],dp[1] = 0,1

        #遍历顺序
        for i in range(2,n+1):

            #递推公式
            dp[i] = dp[i-1] + dp[i-2]

        #打印dp数组
        return dp[n]