# 爬楼梯

## 📖 题目链接
[LeetCode 70. Climbing Stairs](https://leetcode.com/problems/climbing-stairs/description/)



## 📌 思路
1. 确定dp数组
    - i阶台阶需要 dp[i]个步骤，第n 需要dp[n]
    - dp[i]
2. 确定递推公式
    - dp[i] = dp[i-1] + dp[i-2]
3. dp数组的初始化
    - dp[1],dp[2] = 1,2
4. 遍历顺序
5. 打印dp数组



## 🧩 代码实现
```python
class Solution:
    def climbStairs(self, n: int) -> int:

        if n < 3 :
            return n
        # 从第零层开始，所以我们需要n+1 长度的数组
        # 如果n是5， 那数组里会有0，1，2，3，4，5层的步数，也就是长度6
        dp = [0] * (n+1)

        

        # 从第三层开始，每一层都符合递推公式，
        # 因为我们需要dp[n]所以我们需要遍历到n+1 因为右边是开的，只有遍历到n+1 才能便利到n 
        for i in range(3,n+1):
            dp[i] = dp[i-1] + dp[i-2]
        
        return dp[n]