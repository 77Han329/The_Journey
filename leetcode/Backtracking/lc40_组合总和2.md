# 组合总和2

## 📖 题目链接
[LeetCode 40. Combination2](https://leetcode.com/problems/combination-sum-ii/)





## 🧩 代码实现
```python
class Solution:
    def combinationSum2(self, candidates: List[int], target: int) -> List[List[int]]:
        res = []
        path = []

        candidates.sort()

        def backtracking(target,candidates,start_idx):
            if sum(path) > target:
                return
            elif sum(path) == target:
                res.append(path[:])
                return
            for i in range(start_idx,len(candidates)):
                if i>start_idx and candidates[i] == candidates[i-1]:
                    continue
                path.append(candidates[i])
                backtracking(target,candidates,i+1)
                path.pop()
                 
        
        backtracking(target,candidates,0)

        return res
            
            
            
```

## 🧩 剪枝后re

        