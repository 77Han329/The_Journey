# 🌲 LeetCode 101 - 移动零

## 📘 题目描述

给定一个未排序的整数数组 nums ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。

请你设计并实现时间复杂度为 O(n) 的算法解决此问题。

 

示例 1：

输入：nums = [100,4,200,1,3,2]
输出：4
解释：最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。
示例 2：

输入：nums = [0,3,7,2,5,8,4,6,0,1]
输出：9
示例 3：

输入：nums = [1,0,1,2]
输出：3
## 💡 解题思路

这道题目有两个核心点：
1. 第一步是需要找到所有的可能的连续序列
2. 做法就是判断每一个数字是否能作为序列的开头
    - 可以作为开头的话就找n+1是不是在数组中，在的话就长度➕1然后就可以记录这个连续序列的长度
    - 不能作为开头就跳过
## 🧱 代码实现

```python
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:

        nums = set(nums)

        max_length = 0
        
        for n in nums:
            length = 0
            # 当前的数字不能作为开头数字，不处理
            if n-1 in nums:
                continue 
            #当千的数字可以作为开头数字处理
            else:
                length +=1
                while n+1 in nums:
                    length +=1
                    n+=1
                max_length = max(length,max_length)

        return max_length

```

```python
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:

        nums = set(nums)

        max_length = 0

        for n in nums:
            length = 0
            # 当前数可以作为开头数字
            # 当前数-1 不存在
            if n - 1 not in nums:
                length +=1
                while n+1 in nums:
                    length+=1
                    n+=1
                max_length = max(length, max_length)
        
        return max_length